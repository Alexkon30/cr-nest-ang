type Lesson {
    _id: Int
    discipline: String
    theme: String
    groups: String # [Group!]
    teachers: [User!]
    room: String
    dateStart: String
    dateEnd: String
    type: String
}

type Query {
    lessons(dateStart: String, dateEnd: String): [Lesson!]
}